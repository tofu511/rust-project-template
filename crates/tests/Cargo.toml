[package]
name = "tests"
version = "0.1.0"
edition = "2024"

[dependencies]
application = { path = "../application" }
adapters = { path = "../adapters" }
domain = { path = "../domain" }
common = { path = "../common" }
contracts-kafka = { path = "../contracts-kafka" }
