[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dev-dependencies]
domain = { path = "../domain" }
ports = { path = "../ports" }
application = { path = "../application" }
observability = { path = "../observability" }

